/* 结算页样式 */
.container {
  padding: 32rpx;
  min-height: 100vh;
  background: var(--color-bg);
}

/* 结算头部 */
.settle-header {
  text-align: center;
  margin-bottom: 32rpx;
}

.settle-title {
  font-size: 48rpx;
  font-weight: 800;
  color: var(--color-text);
  display: block;
}

.settle-subtitle {
  font-size: 26rpx;
  color: var(--color-text-secondary);
  margin-top: 8rpx;
  display: block;
}

/* 赢家展示 */
.winner-showcase {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 40rpx;
  margin-bottom: 32rpx;
  background: linear-gradient(135deg, rgba(212, 167, 44, 0.1) 0%, rgba(212, 167, 44, 0.02) 100%);
  border-radius: 32rpx;
  border: 2rpx solid rgba(212, 167, 44, 0.2);
  position: relative;
}

.winner-crown {
  font-size: 64rpx;
  margin-bottom: 16rpx;
  animation: crownBounce 2s ease-in-out infinite;
}

@keyframes crownBounce {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-12rpx); }
}

.winner-avatar {
  width: 128rpx;
  height: 128rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 16rpx;
  border: 6rpx solid rgba(212, 167, 44, 0.5);
  overflow: hidden;
}

.winner-avatar image {
  width: 100%;
  height: 100%;
}

.winner-avatar .avatar-text {
  font-size: 52rpx;
  color: #FFFFFF;
  font-weight: 700;
}

.winner-name {
  font-size: 36rpx;
  font-weight: 700;
  color: var(--color-text);
}

.winner-label {
  font-size: 24rpx;
  color: var(--color-accent);
  font-weight: 600;
  margin-top: 4rpx;
  background: rgba(212, 167, 44, 0.15);
  padding: 4rpx 24rpx;
  border-radius: 20rpx;
}

.winner-score {
  font-size: 56rpx;
  font-weight: 800;
  color: var(--color-success);
  margin-top: 12rpx;
}

/* 排名卡片 */
.card {
  background: var(--color-card);
  border-radius: 24rpx;
  padding: 32rpx;
  margin-bottom: 24rpx;
  box-shadow: 0 4rpx 16rpx var(--color-shadow);
}

.card-title {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--color-text);
  margin-bottom: 24rpx;
}

.card-desc {
  font-size: 24rpx;
  color: var(--color-text-secondary);
  margin-top: -16rpx;
  margin-bottom: 24rpx;
}

.ranking-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

.ranking-item {
  display: flex;
  align-items: center;
  padding: 16rpx 0;
  border-bottom: 1rpx solid var(--color-border);
}

.ranking-item:last-child {
  border-bottom: none;
}

.rank-pos {
  width: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}

.rank-num {
  font-size: 28rpx;
  font-weight: 600;
  color: var(--color-text-secondary);
}

.rank-medal {
  font-size: 40rpx;
}

.rank-player {
  display: flex;
  align-items: center;
  gap: 16rpx;
  flex: 1;
}

.rank-avatar {
  width: 60rpx;
  height: 60rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
  flex-shrink: 0;
}

.rank-avatar image {
  width: 100%;
  height: 100%;
}

.rank-avatar text {
  font-size: 24rpx;
  color: #FFFFFF;
  font-weight: 600;
}

.rank-name {
  font-size: 28rpx;
  color: var(--color-text);
  font-weight: 500;
}

.rank-score-area {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
}

.rank-score {
  font-size: 36rpx;
  font-weight: 700;
}

.rank-score.positive { color: var(--color-success); }
.rank-score.negative { color: var(--color-danger); }

.rank-amount {
  font-size: 22rpx;
  color: var(--color-accent);
  font-weight: 500;
}

/* 转账方案 */
.transfer-list {
  display: flex;
  flex-direction: column;
  gap: 28rpx;
}

.transfer-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.transfer-from, .transfer-to {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8rpx;
  min-width: 120rpx;
}

.transfer-avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
}

.transfer-avatar image, .transfer-avatar-img {
  width: 100%;
  height: 100%;
  border-radius: 50%;
}

.transfer-avatar text {
  font-size: 24rpx;
  color: #FFFFFF;
  font-weight: 600;
}

.transfer-name {
  font-size: 24rpx;
  color: var(--color-text);
  font-weight: 500;
}

.transfer-arrow {
  display: flex;
  align-items: center;
  gap: 8rpx;
  flex: 1;
  justify-content: center;
  position: relative;
}

.arrow-line {
  width: 80rpx;
  height: 3rpx;
  background: var(--color-primary);
}

.arrow-amount {
  font-size: 28rpx;
  font-weight: 700;
  color: var(--color-accent);
  position: absolute;
  top: -36rpx;
}

.arrow-head {
  font-size: 28rpx;
  color: var(--color-primary);
  font-weight: 700;
}

/* AI摘要 */
.ai-summary-card {
  background: linear-gradient(135deg, var(--color-card) 0%, rgba(26, 107, 74, 0.03) 100%);
}

.ai-summary-content {
  min-height: 120rpx;
}

.ai-summary-text {
  font-size: 28rpx;
  color: var(--color-text);
  line-height: 1.8;
  white-space: pre-wrap;
}

/* 按钮组 */
.btn-group {
  margin-top: 32rpx;
  margin-bottom: 48rpx;
}

.btn-share {
  margin-bottom: 24rpx;
}

.btn-row {
  display: flex;
  gap: 20rpx;
}

.btn-row .btn-outline {
  flex: 1;
}

/* 模拟广告区 */
.ad-mock-zone {
  margin-top: 32rpx;
  display: flex;
  flex-direction: column;
  gap: 16rpx;
  margin-bottom: 48rpx;
}
.ad-mock-banner {
  background: #f5f5f5;
  border-radius: 12rpx;
  padding: 16rpx 20rpx;
  position: relative;
  border: 1rpx solid #e8e8e8;
  display: flex;
  align-items: center;
}
.ad-mock-badge {
  position: absolute;
  top: 8rpx; right: 8rpx;
  font-size: 16rpx; color: #999;
  background: #eee; padding: 2rpx 8rpx;
  border-radius: 4rpx;
}
.ad-mock-body { display: flex; align-items: center; gap: 16rpx; }
.ad-mock-icon {
  width: 64rpx; height: 64rpx;
  border-radius: 10rpx;
  background: linear-gradient(135deg, #ddd, #bbb);
  flex-shrink: 0;
}
.ad-mock-text { flex: 1; }
.ad-mock-title { font-size: 24rpx; color: #333; font-weight: 600; display: block; }
.ad-mock-desc { font-size: 20rpx; color: #999; display: block; margin-top: 4rpx; }
.ad-mock-reward {
  background: linear-gradient(135deg, #FFF7E6, #FFEDD5);
  border-radius: 12rpx;
  padding: 20rpx;
  border: 1rpx solid #F5D0A9;
  display: flex; align-items: center; gap: 12rpx;
}
.ad-mock-reward-icon { font-size: 32rpx; }
.ad-mock-reward-text { flex: 1; font-size: 24rpx; color: #8B5E3C; font-weight: 600; }
.ad-mock-reward-btn {
  padding: 8rpx 24rpx; background: #FF8C00;
  color: #fff; font-size: 22rpx; font-weight: 600;
  border-radius: 20rpx;
}
